## Global E2E Verification Principle

- E2E 테스트의 핵심 목표는 사용자가 해볼 수 있는 모든 테스트를 사전에 모두 검증하는 것이다.
- 모든 프로젝트에서 사용자를 대신해 사전에 해볼 수 있는 모든 실제 흐름을 검증한다.
- 사용자의 테스트 부담을 최대한 줄여, 사용자가 직접 반복 검증하지 않아도 충분하다고 판단할 수 있을 만큼 에이전트와 하네스가 먼저 실행하고 증거를 남긴다.
- 구현 단계의 완료 기준은 정상 흐름 구현이 아니라 정상/비정상/오류/장애·복구 흐름까지 업무적으로 납득 가능한 UX와 데이터 무결성을 갖추는 것이다.
- 최종 E2E 검증은 정상 흐름만으로 완료 처리하지 않는다. 정상 흐름만 검증한 테스트는 불충분한 테스트로 간주한다.
- 기능의 업무 의미를 기준으로 정상/비정상/오류/장애·복구 흐름을 케이스 매트릭스로 정의한다.
- 비정상 흐름은 기존 제품 또는 이전 버전의 업무 처리 방식이 있으면 먼저 벤치마킹한다. AIMS/UIX4 작업에서는 UIX3의 업무 흐름을 확인하고, UIX3가 커버하지 않는 케이스는 사용자 UX 관점의 최선 처리안을 구현하거나 별도 보고한다.
- 정상/비정상 흐름에서 나온 결과는 사용자가 보기에 합리적인 UX여야 한다. 에러가 기술적으로 맞더라도 사용자가 다음 행동을 예측할 수 없으면 완료가 아니다.
- 핵심 업무 데이터 무결성에 영향을 주는 케이스는 E2E로 통과시킨다.
- E2E가 불가능하거나 E2E가 아닌 단위/통합 테스트로 대체한 케이스는 완료 보고서에 대체 사유와 잔여 리스크를 명시한다.
- 누락된 E2E 케이스가 있으면 완료, PASS, 커밋 승인 요청을 하지 않는다.

## Global Regression Test Rule

- 구현 변경은 반드시 해당 변경을 고정하는 회귀 테스트를 함께 추가하거나 기존 테스트를 수정한다.
- 회귀 테스트는 변경된 동작이 다시 깨졌을 때 실패해야 하며, 단순 스냅샷 갱신이나 의미 없는 기대값 변경으로 대체하지 않는다.
- UI 변경은 사용자 흐름, 스크롤/hover/선택/오류/빈값 등 실제로 깨질 수 있는 상태를 포함한 E2E 또는 동등한 검증을 추가한다.
- 데이터 무결성, 계산식, Excel 다운로드, 외부 조회값 해석처럼 업무 판단에 영향을 주는 변경은 정상 케이스뿐 아니라 누락/0/미확인/오류 등 회귀 위험 케이스를 포함한다.
- 회귀 테스트를 추가할 수 없는 경우에는 완료 보고서에 대체 검증 방법, 추가하지 못한 사유, 잔여 리스크를 명시한다.
- 회귀 테스트가 누락된 구현 변경은 완료, PASS, 커밋 승인 요청을 하지 않는다.

## Global Critical Judgment Rule

- 에이전트는 사용자의 지시를 단순 수행 대상으로만 보지 않는다.
- 요구 목적, 업무 맥락, UX, 데이터 무결성, 기술 리스크에 비추어 지시가 부적절하면 즉시 이의를 제기하고 더 나은 대안을 제안한다.
- 사용자가 제안한 구현 방식은 목적 달성에 적합할 때만 채택하며, 채택하거나 거절하는 이유를 명확히 설명한다.
- 특히 UI/UX, 데이터 해석, 저장/삭제/외부 연동, 회귀 위험이 큰 작업에서는 비판적 검토 없이 구현 완료로 보지 않는다.
- 비판은 작업을 멈추기 위한 것이 아니라 더 나은 결정과 구현으로 이어져야 한다.

## UX/UI Review Collaboration Rule

- 사용자 화면, 시각 상태, 색상, 레이아웃, 인터랙션, 업무 의미가 있는 UI를 변경할 때는 UX/UI 전문 서브에이전트를 함께 사용한다.
- UX/UI 에이전트는 시각적 위계, 상태 구분, 업무 의미 전달, 접근성, 스캔 효율, 오류/선택/hover 상태 충돌을 검토한다.
- 구현 에이전트는 UX/UI 제안을 그대로 적용하기보다 실제 코드 구조, 테스트, E2E 검증 결과와 함께 판단해 최종안을 적용한다.
- 단순 빌드, 데이터 로직, 비시각적 리팩터링에는 필수로 호출하지 않는다.

## Global UX Design Consistency Rule

- 디자인 일관성은 프로젝트 전체에서 반드시 지켜야 하는 핵심 UX 원칙이다.
- 색상, 경계선, 배경, 하이라이트, hover, 선택 상태는 설계자의 의미 체계를 사용자에게 전달하는 수단이다. 같은 의미는 항상 같은 시각 표현을 사용해야 한다.
- 영역 색상은 소속과 업무 의미만 표현하고, 상태 색상은 선택/hover/오류/미확인 같은 상태만 표현한다. 서로 다른 의미를 같은 색으로 섞거나, 같은 의미를 위치별로 다르게 표시하지 않는다.
- 경계선은 구조를 설명하는 보조 수단이다. 테이블/업무 화면의 기본 경계는 컴팩트한 1px 중립선으로 유지하고, 두꺼운 선을 반복해서 시각적 소음을 만들지 않는다.
- 영역 구분이 필요하면 선을 두껍게 하기보다 배경 톤, 여백, 얇은 accent, 텍스트 위계를 조합한다.
- 상호작용 상태의 우선순위는 `교차점 > 선택 행/열 > hover 행/열 > 데이터 신뢰 상태 > 영역 소속 색 > 기본 그리드` 순서로 설계한다. 단, 데이터 신뢰 상태가 사용자의 값 해석에 영향을 주는 경우 hover만으로 의미가 사라지면 안 된다.
- 병합되어 보이는 셀은 내부 경계선을 보여주지 않고, 실제 행 단위 정보가 있는 셀만 내부 경계를 유지한다.
- UI 변경 완료 전에는 대표 화면만 보지 않는다. 수평/수직 스크롤 위치, hover/선택/교차점, 합계/카테고리/빈값/오류 상태를 캡처 또는 E2E로 확인해 디자인 일관성 위반이 없음을 검수한다.
- 디자인 일관성 원칙을 지키지 않은 UI는 기능이 동작하더라도 완료로 보지 않는다.

## Global Git Commit Message Rule

- 모든 프로젝트에서 git 커밋 메시지는 기본적으로 한국어로 작성한다.
- 사용자가 명시적으로 다른 언어를 요청한 경우에만 그 언어를 사용한다.

## Global Build Temp Directory Rule

- 모든 프로젝트에서 컴파일, 번들링, 패키징, PyInstaller, Inno Setup, npm/vite build, electron build처럼 대량 임시 파일을 만드는 작업은 반드시 C 드라이브 임시 작업 폴더에서 수행한다.
- D 드라이브, 네트워크 드라이브, HDD/SATA 드라이브를 빌드 work/temp/cache 디렉터리로 사용하지 않는다.
- 기본 빌드 임시 루트는 `C:\codex-build-temp\{project-name}`이다.
- 최종 산출물만 필요 시 원래 프로젝트의 `dist`, `release`, `build` 산출물 위치로 복사한다.
- 도구가 지원하면 `--workpath`, `--specpath`, `--distpath`, `TMP`, `TEMP`, `TMPDIR`, `npm_config_cache` 등을 C 드라이브 경로로 지정한다.
- 빌드 명령을 실행하기 전 C 드라이브 빌드 임시 폴더를 만들고, 빌드가 끝난 뒤 필요한 최종 산출물만 프로젝트 폴더로 복사한다.
