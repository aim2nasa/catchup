# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 성격

캐치업코리아 법인의 업무를 분석하고 IT 기반 워크플로우로 개선하기 위한 프로젝트. 일반 소프트웨어 개발 repo가 아니라 **업무 분석 + 점진적 시스템화** 작업이 동시에 일어나는 공간이다.

- 운영 도메인: cafe24 기반 쇼핑몰. 모든 상품이 cafe24에 등록되어 있는 것으로 파악됨.
- 현행 업무: 판매 상품 관리가 별도 엑셀 파일 수기 방식으로 처리되고 있음.
- 작업 방향: 현행 업무를 먼저 이해 → 비효율/개선 지점 식별 → IT로 대체·자동화할 부분을 단계적으로 코드화.

## 디렉토리 구조

- `docs/` — 분석/참고 문서. 예: `docs/cafe24/CONTEXT.md` (cafe24 OpenAPI 연동 메모).
- `web/` — PC/모바일 웹 산출물.
- `python/` — 파이썬 스크립트/도구.

초기 단계라 구조는 단순하게 유지. 새로운 카테고리가 필요해질 때만 추가.

## 환경

- OS: Windows. Python 실행 시 `py` 런처 사용 (`python` 미설치 가능성).
- cafe24 자격증명(`token.json`, `.env` 등)은 `.gitignore`로 차단됨. 절대 커밋 금지.

## 빌드/실행/테스트 명령

현재 정의된 명령 없음. 산출물이 추가될 때 이 섹션에 누적 기록.

## 작업 시 고려사항

- "엑셀 업무를 그대로 자동화"하기보다, 그 엑셀이 무엇을 위해 존재하는지(어떤 의사결정/리포트를 위한 것인지)를 먼저 파악한 뒤 개선안을 제안할 것.
- 도메인 이해가 깊어지거나 워크플로우 결정이 내려지면 `README.md`의 "현재 파악된 상황" 항목과 `docs/` 산하 문서를 함께 갱신.
